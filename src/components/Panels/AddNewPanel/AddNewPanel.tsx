import React from 'react';

type AddNewProps = {

}

export default function AddNewPanel({}:AddNewProps) {
    return <div>
        <input type='text' />
        <button onClick={()=> {}}>Save</button>
    </div>
}
